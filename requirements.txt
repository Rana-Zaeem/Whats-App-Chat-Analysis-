######################################################################
# Runtime dependencies for WhatsApp Chat Analyzer
# Kept lean and compatible with Python 3.10; all are prebuilt wheels.
######################################################################

streamlit==1.22.0
pandas==1.3.5
numpy==1.21.6
matplotlib==3.5.3
seaborn==0.12.2
nltk==3.7

# Required by the app but previously missing
plotly==5.13.1
wordcloud==1.9.2
emoji==2.2.0
urlextract==1.8.0
textblob==0.17.1

# Notes:
# - scikit-learn is NOT required; we removed unused imports to keep install fast.
# - WordCloud pulls Pillow automatically.
# - If deploying to environments with stricter pins, see DEPLOYMENT.md.
